# Netlify configuration file

[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

# Temporarily disable redirects for debugging
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200